<h1>Items</h1>
<%= link_to "Create new item", new_item_path if authenticated? %>

<div id="items">
  <% @items.each do |item| %>
    <div>
        <%= link_to item.name, item %>
    </div>
  <% end %>
</div>